apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-script
data:
  init.sql: |
    SELECT 'CREATE DATABASE notesdb' WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'notesdb')\gexec
